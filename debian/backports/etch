#!/bin/bash
#
# Hook for automatic backports at buildserver.net
#
# Target dist: Debian Etch

# Downgrade debhelper to 5.0.42 compat level 
sed -i -e  's#^\(Build-Depends:.*\)debhelper[^,$]*[\ ,$]\+\(.*\)$#\1debhelper (>= 5.0.42), \2#' debian/control

exit 0
